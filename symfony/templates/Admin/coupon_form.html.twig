{% extends 'admin/_base_modern.html.twig' %}

{% block page_title %}{{ coupon is null ? 'Ajouter' : 'Modifier' }} un Coupon{% endblock %}
{% block page_subtitle %}{{ coupon is null ? 'Creer un nouveau coupon' : 'Modifier la configuration du coupon' }}{% endblock %}

{% block main %}
<div style="padding: 2rem;">
    <div class="form-container">
        <h1><i class="fas fa-ticket-alt"></i> {{ coupon is null ? 'Ajouter' : 'Modifier' }} un Coupon</h1>

        {{ form_start(form, { attr: { novalidate: 'novalidate', class: 'modern-form js-ajax-form', id: 'couponForm' } }) }}
            <div class="form-error-message" data-form-error style="display:none"></div>
            <div class="form-grid">
                <div class="form-group">
                    {{ form_label(form.code) }}
                    {{ form_widget(form.code) }}
                    {{ form_errors(form.code) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.type) }}
                    {{ form_widget(form.type) }}
                    {{ form_errors(form.type) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.valeur) }}
                    {{ form_widget(form.valeur) }}
                    {{ form_errors(form.valeur) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.montantMinimumPanier) }}
                    {{ form_widget(form.montantMinimumPanier) }}
                    {{ form_errors(form.montantMinimumPanier) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.usageMax) }}
                    {{ form_widget(form.usageMax) }}
                    {{ form_errors(form.usageMax) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.usageCount) }}
                    {{ form_widget(form.usageCount) }}
                    {{ form_errors(form.usageCount) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.dateExpiration) }}
                    {{ form_widget(form.dateExpiration) }}
                    {{ form_errors(form.dateExpiration) }}
                </div>

                <div class="form-group form-switch-group">
                    {{ form_label(form.actif) }}
                    <div class="switch-row">
                        {{ form_widget(form.actif) }}
                        <span>Coupon actif</span>
                    </div>
                    {{ form_errors(form.actif) }}
                </div>
            </div>

            <div class="form-actions">
                <a href="{{ path('admin_coupons') }}" class="btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour
                </a>
                <button type="submit" class="btn-primary">
                    <i class="fas fa-save"></i> {{ coupon is null ? 'Ajouter' : 'Modifier' }}
                </button>
            </div>
        {{ form_end(form) }}
    </div>
</div>

<style>
.form-container {
    background: white;
    padding: 2rem;
    border-radius: 16px;
    box-shadow: var(--shadow-md);
    max-width: 800px;
    margin: 0 auto;
}

.form-container h1 {
    color: var(--primary-color);
    margin-bottom: 2rem;
    font-size: 1.75rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.modern-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
}

.form-group {
    display: flex;
    flex-direction: column;
}

.form-group label {
    font-weight: 600;
    color: var(--dark-color);
    margin-bottom: 0.5rem;
}

.form-group input,
.form-group select,
.form-group textarea {
    padding: 0.875rem 1.25rem;
    border: 2px solid var(--border-color);
    border-radius: 12px;
    font-size: 1rem;
    transition: var(--transition);
    background: white;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(22, 86, 63, 0.1);
}

.form-group .invalid-feedback,
.form-group .form-error-message {
    color: var(--danger-color);
    font-size: 0.875rem;
    margin-top: 0.25rem;
    display: block;
}

.form-group .is-invalid,
.form-group input.is-invalid,
.form-group select.is-invalid,
.form-group textarea.is-invalid {
    border-color: var(--danger-color);
    box-shadow: 0 0 0 3px rgba(220, 53, 69, 0.1);
}

.form-group input:focus.is-invalid,
.form-group select:focus.is-invalid,
.form-group textarea:focus.is-invalid {
    border-color: var(--danger-color);
    box-shadow: 0 0 0 3px rgba(220, 53, 69, 0.25);
}

.form-switch-group input[type="checkbox"] {
    width: 18px;
    height: 18px;
    margin: 0;
    accent-color: var(--primary-color);
}

.switch-row {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    background: #f5faf7;
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 0.7rem 0.9rem;
    color: var(--text-muted);
    font-weight: 600;
}

.form-actions {
    display: flex;
    gap: 1rem;
    justify-content: flex-end;
    margin-top: 2rem;
}

.btn-secondary {
    background: var(--text-muted);
    color: white;
    padding: 0.875rem 1.75rem;
    text-decoration: none;
    border-radius: 12px;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: var(--transition);
    font-weight: 600;
}

.btn-secondary:hover {
    background: #5a6268;
    transform: translateY(-2px);
}

@media (max-width: 768px) {
    .form-grid {
        grid-template-columns: 1fr;
    }

    .form-actions {
        flex-direction: column;
    }
}
</style>
<script>
(() => {
  const form = document.getElementById('couponForm');
  if (!form) return;

  const clearErrors = () => {
    form.querySelectorAll('.is-invalid').forEach(el => el.classList.remove('is-invalid'));
    form.querySelectorAll('.ajax-field-error').forEach(el => el.remove());
    const global = form.querySelector('[data-form-error]');
    if (global) { global.style.display = 'none'; global.textContent = ''; }
  };

  const showErrors = (errors) => {
    const global = form.querySelector('[data-form-error]');
    Object.entries(errors || {}).forEach(([field, messages]) => {
      const msg = Array.isArray(messages) && messages.length ? messages[0] : 'Valeur invalide';
      if (field === '_form') {
        if (global) { global.style.display = 'block'; global.textContent = msg; }
        return;
      }
      const input = form.querySelector(`[name$="[${field}]"]`);
      if (!input) return;
      input.classList.add('is-invalid');
      const err = document.createElement('div');
      err.className = 'form-error-message ajax-field-error';
      err.textContent = msg;
      input.insertAdjacentElement('afterend', err);
    });
  };

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    clearErrors();
    const data = new FormData(form);
    const res = await fetch(form.action, {
      method: form.method || 'POST',
      body: data,
      headers: { 'X-Requested-With': 'XMLHttpRequest' }
    });
    const payload = await res.json().catch(() => ({}));
    if (!res.ok || payload.ok === false) {
      showErrors(payload.errors || { _form: ['Donnees invalides.'] });
      return;
    }
    window.location.href = payload.redirect_url || window.location.href;
  });
})();
</script>
{% endblock %}
