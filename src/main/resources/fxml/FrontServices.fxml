<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<ScrollPane xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
        fx:controller="org.example.controller.FrontServicesController"
        fitToWidth="true" fitToHeight="true" pannable="true"
        hbarPolicy="AS_NEEDED" vbarPolicy="AS_NEEDED" styleClass="page-scroll">
    <content>
    <BorderPane styleClass="admin-root front-shop-page">
    <top>
        <VBox spacing="12" styleClass="admin-header-wrap">
            <fx:include fx:id="shopNav" source="FrontShopNavBar.fxml"/>

            <HBox spacing="10" styleClass="filters-row">
                <TextField fx:id="searchField" promptText="Recherche service..." HBox.hgrow="ALWAYS" styleClass="filter-field"/>
                <ComboBox fx:id="typeFilter" promptText="Type" styleClass="filter-field"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <StackPane styleClass="module-content-wrap">
            <VBox spacing="14" styleClass="table-card">
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <VBox styleClass="stat-card-mini" spacing="3">
                        <Label text="Services" styleClass="stat-label-mini"/>
                        <Label fx:id="totalServicesLabel" text="0" styleClass="stat-value-mini"/>
                    </VBox>
                    <Label text="Services disponibles" styleClass="admin-title"/>
                </HBox>

                <ScrollPane fitToWidth="true" styleClass="admin-table" VBox.vgrow="ALWAYS">
                    <content>
                        <FlowPane fx:id="servicesFlow" hgap="12" vgap="12" prefWrapLength="920" style="-fx-padding: 12;"/>
                    </content>
                </ScrollPane>
            </VBox>
        </StackPane>
    </center>
    </BorderPane>
    </content>
</ScrollPane>
